import * as React from "react";
import * as Types from "./types";

declare function UnknownComponent4(props: {
  as?: React.ElementType;
  emptyDescription?: React.ReactNode;
  emptyHeading?: React.ReactNode;
}): React.JSX.Element;
